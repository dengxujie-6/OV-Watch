# Readme

我在智能手表项目中遇到过一个比较难定位的问题：Menu 页面连续滑动时会偶发卡住，但页面静止时正常，并且长按按键仍然能关机。

我最开始先怀疑是 FreeRTOS 任务栈溢出，所以检查了栈溢出 Hook，但复现时没有命中；再结合按键关机仍然可用，判断不是整机死机，而是 GUI 任务或显示刷新链路异常。

项目 LCD 使用 SPI DMA 双缓冲刷新。LVGL 发起刷新后，DMA 完成中断会释放信号量，LVGL 再通过 flush_wait_cb 等待信号量并调用 lv_display_flush_ready()，表示这一帧刷新完成。

我在刷新请求、DMA 完成回调、等待信号量、通知 LVGL 刷新完成等节点增加计数器。复现时没有发现等待 DMA 信号量超时，说明不是 DMA 完成通知丢失导致的卡死。

然后我给 Menu 事件和 LVGL 主任务加阶段号，确认菜单点击逻辑已经返回，GUI 任务停在 lv_timer_handler() 内部。继续打开 LVGL 日志后，捕获到了 lv_malloc 申请约 4 KB 临时内存失败。

最后我做 A/B 测试，保留菜单缩放效果，只移除圆角裁剪 clip_corner，问题暂时不再复现。说明连续滑动时，缩放和圆角裁剪叠加进入了较高成本的绘制路径，增加了 LVGL 临时 Layer 内存申请压力。

最终我通过减少高成本样式组合修复问题，并保留了内存监控、刷新链路计数器和压力测试方法，方便后续回归验证。


最有可能被问的，不是 LVGL 源码细节，而是下面这 **7 个问题**。先把这些答熟，性价比最高。

你不需要主动讲 `tp/rp/dp`、双缓冲内部时序等细节；只有面试官继续深挖时再展开。

## 1. 这个 Bug 的现象是什么？

Menu 页面在连续滑动、反复触发按压效果时会偶发卡住，静止时正常。卡住后页面不再刷新，但长按按键仍能关机。

这说明不是简单的整机完全死机，更像 GUI 任务、LVGL 刷新流程或显示链路出现异常。

---

## 2. 你为什么先怀疑 DMA 刷新链路？

因为 LCD 使用 SPI DMA 双缓冲异步刷新。

LVGL 触发刷新后，显示驱动启动 DMA；DMA 完成中断释放信号量；LVGL 再等待这个信号量，并调用 `lv_display_flush_ready()`，告诉 LVGL 当前帧已经完成。

如果 DMA 完成中断没进、信号量没释放，或者没有通知 LVGL 刷新完成，LVGL 可能一直认为上一帧没结束，后续页面就不再刷新。

连续滑动会显著提高刷新频率，所以这条异步链路是我优先怀疑的方向。

---

## 3. 你怎么验证 DMA 链路有没有问题？

我没有只看一个变量，而是在刷新链的关键节点加递增计数器：

* LVGL 发起刷新请求；
* DMA 完成回调；
* LVGL 等待 DMA 信号量；
* `lv_display_flush_ready()` 通知刷新完成；
* 等待信号量是否超时。

正常情况下，这几个计数应该长期接近并能闭环。

如果 DMA 完成通知丢失，通常会看到等待次数增加，但完成回调或 `flush_ready` 没有对应增长，并且会出现等待超时。

实际复现时，没有出现等待超时，最近一次信号量等待也成功，因此没有证据支持“LVGL 卡在等待 DMA 完成”这个假设。

---

## 4. 你为什么还检查了任务栈溢出？

因为嵌入式里任务栈溢出可能表现为页面卡住、日志停止、队列或信号量异常，甚至影响其他任务。

我开启了 FreeRTOS 的栈溢出检测，并实现了栈溢出 Hook。复现问题时没有命中 Hook，所以没有发现被 FreeRTOS 捕获的典型任务栈溢出。

但 Hook 没命中不能绝对排除所有越界写，只能说明当前没有抓到典型的栈溢出。

---

## 5. 后来怎么继续缩小范围？

我在 Menu 按压事件和 `lvglTask` 中加入阶段号。

结果显示：Menu 的按压回调已经执行完，但 `lvglTask` 停在 `lv_timer_handler()` 内部，没有继续返回到后面的任务循环。

这说明问题不在普通的菜单点击回调，而更可能在 LVGL 后续的刷新、绘制或资源处理过程。

---

## 6. 最终拿到了什么直接证据？

我打开 LVGL 的最小化内部日志，复现时抓到了：

`lv_malloc: couldn't allocate memory (4235 bytes)`

这说明 LVGL 在刷新或绘制过程中需要申请约 4 KB 的临时内存，但申请失败。

同时日志显示该问题版本运行时存在较高的内存使用率、较高碎片率，以及偏小的最大连续空闲块。

因此可以确认：问题包含 LVGL 临时内存申请失败，而不是单纯 DMA 等待超时。

---

## 7. 最终如何修复？

我对菜单效果做了单变量 A/B 测试。

原始页面同时使用了 `transform_zoom` 缩放效果和 `clip_corner` 圆角裁剪。测试中我保留缩放效果，只移除圆角裁剪。

移除 `clip_corner` 后，连续滑动压力测试下问题暂时不再复现。

因此我没有简单增加内存池，而是优先降低高频交互时的绘制复杂度，减少临时 Layer 或临时绘制缓冲的申请压力。

后续我保留了 LVGL 内存监控、刷新链路计数器和连续滑动压力测试，用于回归验证。

面试时最容易被连续追问的是这三条：

1. **为什么先查 DMA？**
2. **计数器怎么证明 DMA 没卡住？**
3. **最终如何确认并修复？**

先把这三题讲顺，已经足够覆盖大多数嵌入式面试场景。
